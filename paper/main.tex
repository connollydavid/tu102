% Style: terse technical prose, British English (-ise). Code identifiers,
% SASS/PTX mnemonics, and named APIs stay US English ASCII verbatim.
% Author-style notes (from Connolly XP2010, ECBS2008): cold-open with a
% load-bearing fact, no throat-clearing; cite at the claim, quote borrowed
% phrases inline; impersonal voice (no "we" inflation); name tools plainly,
% no marketing adjectives; hedge honestly in-line; future work is specific
% or absent; short sections that earn their headers.
% Full-paper structure (from Connolly ICSE AST 2009): honest genre label in
% the title; ONE explicit research question, italicised, returned to in the
% conclusion; operational definitions with counting rules before any data;
% walk one worked example end-to-end (bench -> SASS gate -> reps -> median
% -> prior -> deviation) before the bulk tables; every derived statistic
% gets its formula plus one worked numeric example; negative results stay
% in aggregates, typographically marked; a Limitations subsection that
% names biases directly (single rig, single toolchain/driver snapshot, T4
% prior transferability, no independent replication) with mitigations.
% From EUROSPI 2009/2010: the pre-registration move — metrics and gates
% defined in print before data collection; HERE the public git history of
% COVERAGE.md/SCHEMA.md IS the receipt (cite commit dates in Methodology);
% one-line robustness checks ("the use of median would not reverse the
% overall results"); numbered display equations for derived-stat formulas.
% Negative style control: avoid the AI writing tropes catalogued in
% paper/tropes.md (tropes.fyi by ossama.is, imported 2026-06-09) — e.g.
% negative parallelism ("It's not X — it's Y"), "serves as", magic adverbs,
% manufactured fragments, false ranges, signposted conclusions. Any one
% pattern once may pass; clusters are a rewrite trigger.
% Work-segmentation vocabulary is likewise banned from prose, headers, and
% commit subjects: no numbered spans ("Phase 1", "Step 3 of 5", "M2") and
% no deformed plan arithmetic ("5.5"); spans of work get proper names. See
% github.com/connollydavid/no-phase-skill (VOCABULARY.md). Academic section
% numbering produced by LaTeX is typography, not a plan label, and is fine.
%
% Template notes: pdfLaTeX, arXiv-safe (no shell-escape, Type 1 fonts
% embedded). Palatino-descended text/math (newpx) + Inconsolata mono for
% SASS/PTX. One accent colour, used only for section numbers, links, and
% listing keywords — the content is the interface. Prefer plain unit text
% in prose ("1455 MHz"); siunitx belongs in tables. Remember: arXiv upload
% needs the .bbl alongside the .tex.
\documentclass[11pt]{article}

% ---------------- typography ----------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{newpxtext,newpxmath}        % Palatino-descended text + maths
\usepackage[varqu,varl]{zi4}            % Inconsolata for SASS/PTX/code
\usepackage{microtype}
\usepackage[margin=1.1in]{geometry}
\setlength{\parskip}{0pt}
\usepackage{graphicx}

% ---------------- tables & units ----------------
\usepackage{booktabs}
\usepackage{siunitx}
\sisetup{per-mode=symbol, detect-weight=true, table-number-alignment=right}

% ---------------- one accent colour, sparingly ----------------
\usepackage{xcolor}
\definecolor{accent}{HTML}{1F5F5B}      % deep teal
\usepackage{titlesec}
\titleformat{\section}
  {\Large\bfseries}{\textcolor{accent}{\thesection}}{0.8em}{}
\titleformat{\subsection}
  {\large\bfseries}{\textcolor{accent}{\thesubsection}}{0.8em}{}
\titleformat{\subsubsection}
  {\normalsize\bfseries}{\textcolor{accent}{\thesubsubsection}}{0.8em}{}

\usepackage{caption}
\captionsetup{labelfont={bf,small}, font=small, labelsep=period}

% ---------------- listings: SASS / PTX / CUDA ----------------
\usepackage{listings}
\lstdefinelanguage{sass}{
  morekeywords={FFMA,FADD,FMUL,IADD3,IMAD,LOP3,SHF,SEL,ISETP,FSETP,POPC,
    FLO,PRMT,IDP,HFMA2,DADD,DFMA,MUFU,F2F,I2F,F2I,HMMA,IMMA,LDSM,LDG,LDS,
    STG,STS,LDC,BAR,SHFL,VOTE,BRA,EXIT,NOP,CS2R,S2R,MOV,RET},
  sensitive=true,
  morecomment=[l]{//},
}
\lstset{
  language=sass,
  basicstyle=\small\ttfamily,
  keywordstyle=\color{accent}\bfseries,
  commentstyle=\color{black!55},
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  frame=leftline,
  framerule=0.6pt,
  rulecolor=\color{accent!60},
  xleftmargin=1em,
  aboveskip=0.8\baselineskip,
  belowskip=0.8\baselineskip,
}

% ---------------- references & links ----------------
\usepackage[numbers]{natbib}
\usepackage{url}
\usepackage[colorlinks=true, linkcolor=accent, citecolor=accent,
            urlcolor=accent]{hyperref}
\usepackage[capitalise,noabbrev]{cleveref}

% ---------------- title block ----------------
\title{\bfseries An Instruction-Level Characterisation of the NVIDIA
  TU102 GPU\\[0.3em]
  \large\mdseries Latency, Throughput, Memory Hierarchy, and NVLink
  Interconnect on sm\_75}
\author{David Connolly\\[0.2em]\normalsize\texttt{david@connol.ly}}
\date{\normalsize Draft --- \today}

\begin{document}
\maketitle

\begin{abstract}
\noindent
We present an Agner-Fog-style operation table for the NVIDIA TU102 GPU
(compute capability 7.5): 264 measured rows covering instruction latency
and throughput with contention-probed issue-pipe bindings, the complete
memory hierarchy, and the NVLink/PCIe/NCCL interconnect, on a locked-clock
two-GPU rig. Every timed loop is SASS-verified --- the public toolchain
rewrote or deleted measurement kernels eight distinct ways, each caught by
the gate --- and every row carries provenance, priors where they apply,
and published deviations. Three decision-grade questions were
pre-registered in the repository before their data existed. Their
outcomes split sharply: compositions of table rows predict
single-resource kernels within 3.5\% but fail with the wrong sign on an
issue-coupled kernel pair, under-predict a payload-bearing interconnect
primitive until single-warp width and first-read visibility rows replace
streaming floors, and under-predict production dispatch cost by half
against an empty-launch floor whose gap survives four exonerated
mechanisms. Findings new to the
public record include the fma-pipe binding of dp4a, full-rate
f32-accumulate tensor cores on Quadro-positioned TU102, evict-first loads
that still allocate in L1, and an L2 whose request path saturates below
the DRAM streaming rate. The table, benchmarks, and registration history
are public and regenerable.
\end{abstract}

\input{sections/01-introduction}
\input{sections/02-related-work}
\input{sections/03-methodology}
\input{sections/04-hypotheses}
\input{sections/05-sm-pipelines}
\input{sections/06-memory-hierarchy}
\input{sections/07-interconnect}
\input{sections/08-worked-examples}
\input{sections/09-conclusion}
\input{sections/acknowledgments}

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
